# Report & Presentation  [Owner: Nisa + Umay]

Submission is a single `.zip` with the **report + slides** (source code is NOT in
the zip — the report links to this GitHub repo).

## Required report sections (from the assignment)
- [ ] Simulation setup: room, tag layout, robot, camera
- [ ] Motion model + noise parameters used in prediction
- [ ] Sensor model, incl. how the multi-hypothesis likelihood
      `p(z|x) = Σ_{i=1..8} p(z|x, tag_i)` is computed
- [ ] Bayesian-filtering discussion: prior, likelihood, posterior, and how each
      filter step maps to them
- [ ] Screenshots / short videos at 3 stages: initial spread, partial
      convergence, final converged state
- [ ] GitHub link to the source
- [ ] Real Duckiebot demo video (required)

## Presentation
Short talk to Dr. Özgür Erkent + RA Mehmet Muratoğlu, with simulation videos.
